enum SnackBarStatus { error, success }
enum AppTransition {
  fade,
  zoom,
  size,
  left_to_right,
  right_to_left,
  bottom_to_top,
  top_to_bottom,
}
enum AuthType { login, register }

enum LoginStatus {Initial , Loading, loggedIn , NotLoggedIn}